import React from 'react'

import Tooltip from './index'

export default {
  title: 'Libraries|Tooltips',
  component: Tooltip
}

export const Default = () => {
  return <Tooltip />
}

Default.story = {
  name: 'Default'
}
