import React from 'react'

import Modal from './index'

export default {
  title: 'Libraries|Modals',
  component: Modal
}

export const Default = () => {
  return <Modal />
}

Default.story = {
  name: 'Default'
}
