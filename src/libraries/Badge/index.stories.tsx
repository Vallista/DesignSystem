import React from 'react'

import Badge from './index'

export default {
  title: 'Libraries|Badges',
  component: Badge
}

export const Default = () => {
  return <Badge />
}

Default.story = {
  name: 'Default'
}
