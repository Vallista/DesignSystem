import React from 'react'

import Button from './index'

export default {
  title: 'Libraries|Buttons',
  component: Button
}

export const Default = () => {
  return <Button />
}

Default.story = {
  name: 'Default'
}
